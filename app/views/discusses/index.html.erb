<p id="notice"><%= notice %></p>

<h1>Discusses</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>Level</th>
      <th>Response</th>
      <th>Learn user</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @discusses.each do |discuss| %>
      <tr>
        <td><%= discuss.content %></td>
        <td><%= discuss.level %></td>
        <td><%= discuss.response %></td>
        <td><%= discuss.learn_user %></td>
        <td><%= link_to 'Show', discuss %></td>
        <td><%= link_to 'Edit', edit_discuss_path(discuss) %></td>
        <td><%= link_to 'Destroy', discuss, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Discuss', new_discuss_path %>
