<%= form_with(model: learn_user, local: true) do |form| %>
  <% if learn_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(learn_user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% learn_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
